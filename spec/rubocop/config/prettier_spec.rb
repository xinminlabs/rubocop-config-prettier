RSpec.describe Rubocop::Config::Prettier do
  it "has a version number" do
    expect(Rubocop::Config::Prettier::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
